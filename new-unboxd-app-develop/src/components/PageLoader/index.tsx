import { LoaderWrapper } from './styles';
import { LogoLoader } from '../../commons';

const PageLoader = () => (
  <LoaderWrapper>
    <LogoLoader size="md" />
  </LoaderWrapper>
);

export default PageLoader;
